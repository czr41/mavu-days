/** Rows with `showOnLanding` capped for public payloads + positive sync ingest. */
export const PUBLIC_LANDING_REVIEWS_LIMIT = 96;

/** Wider cap for the public “all guest reviews” marketing page only (still `showOnLanding: true`). */
export const PUBLIC_GUEST_REVIEWS_PAGE_LIMIT = 400;
